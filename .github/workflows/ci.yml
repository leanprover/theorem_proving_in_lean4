# This is a basic workflow that is manually triggered

name: Get build artifacts from leanprover

# Controls when the action will run.
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  get-artifacts:
    runs-on: ubuntu-latest
    steps:
    - id: download
      name: Download Lean4 build artifact
      uses: lovettchris/action-download-artifact@v2.15.8
      with:
        # Optional, uploaded artifact names,
        # will download all artifacts if not specified
        # and extract them in respective subdirectories
        # https://github.com/actions/download-artifact#download-all-artifacts
        name: build-Linux Release
        # Optional, directory where to extract artifacts
        path: lean4
        # Optional, GitHub token
        github_token: ${{secrets.MY_PAT_TOKEN}}
        # Required, workflow file name or ID
        workflow: ci.yml
        # Optional, the status or conclusion of a completed workflow to search for
        # Can be one of a workflow conclusion::
        # "failure", "success", "neutral", "cancelled", "skipped", "timed_out", "action_required"
        # Or a workflow status:
        # "completed", "in_progress", "queued"
        # Default: "completed,success"
        workflow_conclusion: success
        # Optional, will get head commit SHA
        # pr: ${{github.event.pull_request.number}}
        # Optional, no need to specify if PR is
        # commit: ${{github.event.pull_request.head.sha}}
        # Optional, will use the branch
        # Optional, will use specified workflow run
        # run_id: 1122334455
        branch: master
        # Optional, defaults to all types
        # event: pull_request
        # Optional, defaults to current repo
        repo: leanprover/lean4
        # Optional, check the workflow run whether it has an artifact
        # then will get the last available artifact from previous workflow
        # default false, just try to download from the last one
        check_artifacts:  true
    - run: |
        tar xvf lean4/lean-4.0.0-linux.tar.gz
        ls -al lean-4.0.0-linux/bin
        lsb_release -a
        ls -al  /lib/x86_64-linux-gnu/libc.so.6
        echo ================= ldd lean
        ldd lean-4.0.0-linux/bin/lean
        echo ================= ldd libleanshared.so
        ldd lean-4.0.0-linux/lib/lean/libleanshared.so
        /lib64/ld-linux-x86-64.so.2 --library-path lean-4.0.0-linux/lib/lean lean-4.0.0-linux/bin/lean --version
        /lib64/ld-linux-x86-64.so.2 --library-path lean-4.0.0-linux/lib/lean lean-4.0.0-linux/bin/lean --help
